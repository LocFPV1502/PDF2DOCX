# Log: 2026-06-07 16:55

## Agent
- Tên / loại agent: Claude Code (opencode)
- Phiên bản: deepseek-v4-flash-free

## Công việc đã làm
- `requirements.txt` — Dependencies: streamlit, pdf2image, pdfminer.six, paddlepaddle, paddleocr, opencv, python-docx
- `src/__init__.py` — Package init
- `src/config.py` — Configuration constants (paths, DPI, pipeline options)
- `src/pdf_processor.py` — PDF analysis, text layer detection, pdf-to-images conversion
- `src/image_processor.py` — OpenCV pipeline (full/light/deskew_only/none)
- `src/ocr_engine.py` — PaddleOCR wrapper (bundled models) + Tesseract fallback
- `src/docx_builder.py` — DOCX generation from text/OCR results
- `app.py` — Streamlit UI (upload, preview, processing, download)
- `run.py` — Bootstrapper (auto-start streamlit + open browser)
- `build.spec` — PyInstaller config for Windows .exe build
- `README.md` — Project documentation
- `.gitignore` — Git ignore rules

## Pipeline status
- Bước đã hoàn thành: 1-12 (TOÀN BỘ)
- Trạng thái: All files created, syntax verified ✅

## Quyết định / thay đổi
- Đã bundle PaddleOCR models vào assets/ocr_models/ (chưa có models, cần download)
- console=True trong build.spec (để thấy log khi debug, có thể đổi False khi release)
- Fallback OCR: Tesseract (chưa implement Windows.Media.Ocr — cần pythonnet)

## Vấn đề còn tồn đọng
- Chưa có PaddleOCR models (cần download trước khi build .exe)
- Chưa có icon.ico (optional)
- Chưa test end-to-end (cần cài dependencies trên Windows)
- Fallback Windows.Media.Ocr chưa implement đầy đủ
